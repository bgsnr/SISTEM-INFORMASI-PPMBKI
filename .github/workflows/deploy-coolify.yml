name: 🚀 Deploy to Coolify

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Deploy
        run: |
          echo "Starting deployment to Coolify..."
          curl -X POST \
            -H "Authorization: Bearer 6|DIi9TaFqeMa0ZFPxGClcFRaI0mCxj3FiaP3TZh92890e308d" \
            -H "Content-Type: application/json" \
            -d '{"uuid":"n4swwo84ss8skwgwoc4oowog"}' \
            https://coolify.raishannan.com/api/v1/deploy
